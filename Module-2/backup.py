def backup(server_name):
    print(f"backup has been started for {server_name}")
    # backup command 1
    # backup command 2
    # conditionals logic if it fails in between
    print(f"backup is failed due to exception in server {server_name}")
    # print message if all succeed
    print(f"backup has been completed for {server_name}.")